function add(n1: number, n2: number) {
  return n1 + n2;
}

function printResult(num: number) {
  console.log("Result: " + num);
}
printResult(add(5, 12));
console.log(printResult(add(5, 12)));
/*Void - standard to be used when constructing function that does not return a value - can be assigned or inferred
Undefined can be used as a datatype but 
*/
